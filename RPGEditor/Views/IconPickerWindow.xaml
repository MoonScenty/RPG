<Window x:Class="RPGEditor.Views.IconPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="아이콘 선택" Height="560" Width="640"
        WindowStartupLocation="CenterOwner"
        FontSize="16">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="번호 검색" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Grid.Column="1" x:Name="FilterTextBox" TextChanged="FilterTextBox_TextChanged" />
        </Grid>

        <ListBox x:Name="IconListBox" Grid.Row="1"
                 BorderThickness="1"
                 ScrollViewer.CanContentScroll="False"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="IconListBox_SelectionChanged">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="40" Height="40" Margin="2" BorderThickness="1" BorderBrush="Transparent"
                            ToolTip="{Binding Index}">
                        <Image Source="{Binding Image}" Width="32" Height="32"
                               RenderOptions.BitmapScalingMode="NearestNeighbor" />
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="SelectedIndexText" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="Gray" />
            <Button x:Name="SelectButton" Content="선택" Width="80" IsDefault="True"
                    IsEnabled="False" Click="SelectButton_Click" />
            <Button Content="취소" Width="80" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
