<Window x:Class="RPGEditor.Views.ChangeMaxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="최대치 변경" Height="170" Width="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontSize="16">
    <StackPanel Margin="16">
        <TextBlock x:Name="CategoryLabel" TextWrapping="Wrap" Margin="0,0,0,8" />
        <TextBlock Text="개수" Margin="0,0,0,4" />
        <TextBox x:Name="CountTextBox" Margin="0,0,0,16" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="확인" Width="70" IsDefault="True" Click="OkButton_Click" />
            <Button Content="취소" Width="70" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
