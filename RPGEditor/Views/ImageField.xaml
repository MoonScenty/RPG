<UserControl x:Class="RPGEditor.Views.ImageField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="220">
    <StackPanel>
        <Border Width="140" Height="90" HorizontalAlignment="Left" BorderBrush="Gray" BorderThickness="1" Background="#1e1e1e">
            <Image x:Name="PreviewImage" Stretch="Uniform" Margin="2" />
        </Border>
        <Grid Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding FileName, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Content="찾아보기..." Margin="6,0,0,0" Click="BrowseButton_Click" />
        </Grid>
    </StackPanel>
</UserControl>
