<UserControl x:Class="RPGEditor.Views.SystemEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="700">
    <StackPanel Margin="16" MaxWidth="420" HorizontalAlignment="Left">
        <TextBlock Text="게임 제목" Margin="0,0,0,4" />
        <TextBox Text="{Binding GameTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

        <TextBlock Text="화면 너비" Margin="0,0,0,4" />
        <TextBox Text="{Binding WindowWidth, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

        <TextBlock Text="화면 높이" Margin="0,0,0,4" />
        <TextBox Text="{Binding WindowHeight, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

        <TextBlock Text="메모" Margin="0,0,0,4" />
        <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="120"
                 VerticalScrollBarVisibility="Auto" />
    </StackPanel>
</UserControl>
