<UserControl x:Class="RPGEditor.Views.AnimationEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:RPGEditor.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>

        <views:EntryListPanel Grid.Column="0" />

        <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch" Background="Transparent" />

        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto"
                      IsEnabled="{Binding Selected, Converter={StaticResource NullToBooleanConverter}}">
            <StackPanel MaxWidth="680" HorizontalAlignment="Left">
                <TextBlock Text="{Binding Selected.Id, StringFormat='ID: {0}'}" Margin="0,0,0,10" />

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="이름" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Selected.Name, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="이펙트 이름 (effects 폴더)" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Selected.EffectName, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="표시 위치" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding Source={StaticResource AnimationDisplayTypeValues}}"
                                  SelectedItem="{Binding Selected.DisplayType}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="배율(%)" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Selected.Scale, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="속도(%)" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Selected.Speed, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="오프셋 X" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Selected.OffsetX, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="오프셋 Y" Margin="0,0,0,4" />
                        <TextBox Text="{Binding Selected.OffsetY, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </Grid>

                <TextBlock Text="회전 (X / Y / Z)" Margin="0,0,0,4" />
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding Selected.Rotation.X, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Column="2" Text="{Binding Selected.Rotation.Y, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Column="4" Text="{Binding Selected.Rotation.Z, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <TextBlock Text="플래시 타이밍" FontWeight="Bold" Margin="0,0,0,6" />
                <DataGrid ItemsSource="{Binding Selected.FlashTimings}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="True"
                          CanUserDeleteRows="True"
                          CanUserSortColumns="False"
                          Height="160"
                          Margin="0,0,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="프레임" Binding="{Binding Frame}" Width="60" />
                        <DataGridTextColumn Header="지속시간" Binding="{Binding Duration}" Width="70" />
                        <DataGridTextColumn Header="R" Binding="{Binding Color[0]}" Width="50" />
                        <DataGridTextColumn Header="G" Binding="{Binding Color[1]}" Width="50" />
                        <DataGridTextColumn Header="B" Binding="{Binding Color[2]}" Width="50" />
                        <DataGridTextColumn Header="A" Binding="{Binding Color[3]}" Width="50" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="사운드 타이밍" FontWeight="Bold" Margin="0,0,0,6" />
                <DataGrid ItemsSource="{Binding Selected.SoundTimings}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="True"
                          CanUserDeleteRows="True"
                          CanUserSortColumns="False"
                          Height="160"
                          Margin="0,0,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="프레임" Binding="{Binding Frame}" Width="60" />
                        <DataGridTextColumn Header="SE 이름" Binding="{Binding Se.Name}" Width="140" />
                        <DataGridTextColumn Header="Pan" Binding="{Binding Se.Pan}" Width="60" />
                        <DataGridTextColumn Header="Pitch" Binding="{Binding Se.Pitch}" Width="60" />
                        <DataGridTextColumn Header="Volume" Binding="{Binding Se.Volume}" Width="60" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Text="메모" Margin="0,0,0,4" />
                <TextBox Text="{Binding Selected.Note, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap" Height="60"
                         VerticalScrollBarVisibility="Auto" />
            </StackPanel>
        </ScrollViewer>

        <GridSplitter Grid.Column="3" Width="8" HorizontalAlignment="Stretch" Background="Transparent" />

        <DockPanel Grid.Column="4">
            <TextBlock DockPanel.Dock="Top" Text="미리보기" FontWeight="Bold" Margin="0,0,0,6" />
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,6,0,0">
                <Button Content="▶ 재생" Width="90" Click="PlayButton_Click"
                        IsEnabled="{Binding Selected, Converter={StaticResource NullToBooleanConverter}}" />
                <Button Content="■ 정지" Width="90" Margin="6,0,0,0" Click="StopButton_Click" />
            </StackPanel>
            <views:AnimationPreviewControl x:Name="Preview" Height="260" />
        </DockPanel>
    </Grid>
</UserControl>
