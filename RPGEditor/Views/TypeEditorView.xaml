<UserControl x:Class="RPGEditor.Views.TypeEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,6,0,0">
                <Button Content="추가" Width="56" Command="{Binding AddCommand}" />
                <Button Content="복제" Width="56" Margin="4,0,0,0" Command="{Binding DuplicateCommand}" />
                <Button Content="삭제" Width="56" Margin="4,0,0,0" Command="{Binding DeleteCommand}" />
                <Button Content="↑" Width="28" Margin="4,0,0,0" Command="{Binding MoveUpCommand}" />
                <Button Content="↓" Width="28" Margin="4,0,0,0" Command="{Binding MoveDownCommand}" />
            </StackPanel>
            <ListBox ItemsSource="{Binding Entries}" SelectedItem="{Binding Selected}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding Group, Mode=OneWay}" FontWeight="Bold" />
                            <Run Text=" · " />
                            <Run Text="{Binding Name, Mode=OneWay}" />
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch" Background="Transparent" />

        <StackPanel Grid.Column="2" MaxWidth="400" HorizontalAlignment="Left"
                    IsEnabled="{Binding Selected, Converter={StaticResource NullToBooleanConverter}}">
            <TextBlock Text="{Binding Selected.Id, StringFormat='ID: {0}'}" Margin="0,0,0,10" />

            <TextBlock Text="그룹 (어디에서 참조하는 유형인지)" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding Source={StaticResource TypeGroupValues}}"
                      SelectedItem="{Binding Selected.Group}" Margin="0,0,0,10" />

            <TextBlock Text="이름" Margin="0,0,0,4" />
            <TextBox Text="{Binding Selected.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

            <TextBlock Text="메모" Margin="0,0,0,4" />
            <TextBox Text="{Binding Selected.Note, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="60"
                     VerticalScrollBarVisibility="Auto" />
        </StackPanel>
    </Grid>
</UserControl>
