<Window x:Class="RPGEditor.Views.NewProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="새 프로젝트" Height="220" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontSize="16">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="프로젝트 이름" Margin="0,0,0,4" />
            <TextBox x:Name="ProjectNameTextBox" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,4">
            <TextBlock Text="생성 위치" Margin="0,0,0,4" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="LocationTextBox" Grid.Column="0" IsReadOnly="True" />
                <Button x:Name="BrowseButton" Grid.Column="1" Content="찾아보기..." Margin="8,0,0,0"
                        Click="BrowseButton_Click" />
            </Grid>
        </StackPanel>

        <TextBlock Grid.Row="2" x:Name="ErrorTextBlock" Foreground="Red" TextWrapping="Wrap" Margin="0,4,0,0" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="만들기" Width="80" IsDefault="True" Click="CreateButton_Click" />
            <Button Content="취소" Width="80" Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
